---
import type { HTMLAttributes } from 'astro/types';
import { type BrutalSize, type BrutalVariant } from '../config';
import { cn, resolveColor } from '../utils/styles';

interface Props extends HTMLAttributes<'button'> {
  color?: string;
  variant?: BrutalVariant;
  size?: BrutalSize;
  fullWidth?: boolean;
}

const {
  color: customColor,
  variant = 'primary',
  size = 'md',
  fullWidth = false,
  class: className,
  ...rest
} = Astro.props;

const color = resolveColor(customColor, variant, 'primary');
const classes = cn('brutal-btn', `brutal-btn--${size}`, fullWidth && 'brutal-btn--full', className);
---

<style define:vars={{ color: color }}>
  button.brutal-btn {
    filter: drop-shadow(5px 5px 0 rgb(0 0 0 / 1));
    background-color: white;
    color: black;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    width: fit-content;
    padding: 0.5rem 1rem;
    border: 2px solid black;
    cursor: pointer;
    font-family: var(--brutal-font-family, 'Sanchez', ui-serif, Georgia, serif);
    font-weight: 700;
    line-height: 1.2;
    transition:
      transform 150ms ease,
      filter 150ms ease,
      background-color 150ms ease;
  }

  button.brutal-btn--sm {
    padding: 0.375rem 0.75rem;
    font-size: 0.875rem;
  }

  button.brutal-btn--md {
    font-size: 1rem;
  }

  button.brutal-btn--lg {
    padding: 0.75rem 1.25rem;
    font-size: 1.125rem;
  }

  button.brutal-btn--full {
    width: 100%;
  }

  button.brutal-btn:hover {
    filter: drop-shadow(3px 3px 0 rgb(0 0 0 / 1));
    background-color: var(--color);
  }

  button.brutal-btn:focus-visible {
    outline: 3px solid black;
    outline-offset: 3px;
    background-color: var(--color);
  }

  button.brutal-btn:active {
    transform: translate(2px, 2px);
    filter: drop-shadow(1px 1px 0 rgb(0 0 0 / 1));
  }

  button.brutal-btn:disabled {
    cursor: not-allowed;
    opacity: 0.65;
    filter: none;
  }

  @media (prefers-reduced-motion: reduce) {
    button.brutal-btn {
      transition: none;
    }
  }
</style>

<button {...rest} class={classes}>
  <slot />
</button>
